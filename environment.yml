name: cbanalysis

channels:
  - conda-forge
  - defaults

dependencies:
  # Core Python
  - python=3.11

  # Data handling
  - numpy
  - pandas
  - pyarrow
  - pyyaml
  - awkward

  # Plotting
  - matplotlib

  # Scientific utilities
  - scipy

  # Development tools
  - pip
  - pip:
      - tqdm
      - tabulate
      - git+https://github.com/usnistgov/FCpy
